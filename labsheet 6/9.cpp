#include <iostream>
#include <string>
#include <stack>

using namespace std;

int main(){

    string directory;
    cin>>directory;

    stack<string > s;

    string folders[100000];
    
    int count = 0;

    for (int i = 0; i < directory.length(); ++i) {
        while
    }

    return 0;
}